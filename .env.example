# Copy this to .env for local testing — but NEVER require('dotenv') in any server file.
# Set these as Railway environment variables for production.
SUPABASE_URL=https://YOUR_PROJECT_ID.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here
PORT=3000
# NODE_TLS_REJECT_UNAUTHORIZED=0   # Uncomment if Supabase TLS errors appear on Railway
